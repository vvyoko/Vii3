- **超大图加载问题**
   - 框架限制每张图片只能占 2G 内存
   - `SkiaSharp` 支持下采样解码
      - 尽量加载可支持的最大分辨率
      - 支持格式：`.bmp`, `.jpg`, `.jpeg`, `.png`,  `.webp`, `.gif`, `.ico`, `.wbmp`
  - `Magick.Net` 不支持
      - 如需加载得先加载原图再缩放
      - 整个流程耗时非常长,所以放弃

- **动图, 动态图片相关**
  - 迷你地图显示的是第一帧,仅用于移动位置
  - 动态照片在部分情况会被锁到 30 帧
    - 绝大部分动态照片中的视频本身没有 60 帧。
    - 猜测是框架合成器问题或解码压力
    - 可隐藏界面元素,缩小图片以查看是否有改善
  - 部分动态照片不遵守规则乱写或不写`XMP`,直接忽略读取
  - 不支持 裁剪, 保存, OCR 等操作
    - 但未屏蔽,调用它会针对图片第一帧进行操作
  - `Avif` 疑似透明度支持有问题
  - `Avif` 未显示正确的帧数
      - 它实际是视频格式,获取数据非常慢
      - 所以暂时先自行实现数据读取
      - 待上游解决或可能无法解决

- **Avif 动画加载慢**
  - 默认情况下不包括 **libmpv** 解析慢处于兜底方案
  - **Avif** 属于视频流,在未包含 **libmpv** 时使用 **Magick.Net** 解析
  - **Magick.Net** 定位为图像处理库,本身可能不擅长解析此格式
    - 见 [MagickImage.ping() of some special avif file very slow](https://github.com/dlemstra/Magick.NET/issues/2005)
    - 该讨论仅针加载基本信息,修复也是仅读取首帧
  - 加载完整动画流上述讨论中的耗时是无法省略的
    - 尝试了边加载边播放,但上述讨论中的耗时还是无法避免
  - 在有可选 **libmpv** 方案下也不会再引入其他库专门处理
  - 如有大量 **Avif 动画** 需求,参考文档下载 **libmpv**
    - **libmpv** 是专业处理视频的库, 非常善于处理视频格式

- **注册 文件夹/压缩包 右键菜单**
  - 关联对象包含：文件夹、ZIP、RAR、CBR、CBZ
  - 除 文件夹 外其他格式必需先设置默认打开方式, 打开方式变更可能需要重新关联
  - 建议第三方文件管理器用户自行实现 (参考文档命令行传递单个或多个文件路径)
  - 或者使用其他自定义右键的软件如 `ContextMenuManager` `ContextMenuForWindows11` 等

- **窗口适应窗口** 会有闪烁
  - 窗口自适应本质触发窗口大小变更操作，该行为由系统全权管理，应用层无自定义处理
  - **窗口适应图片支持缩放** 会加巨这种情况
    - 缩放几乎每次都会修改窗口尺寸
    - 而切换图片,不同图片分辨率可能一致，未必会调整窗口大小
  - 有部分程序它是这样解决的,主程序最大化,除图片,其他元素全部透明且点击穿透
  - 但它完全失去了与其他组件的交互,所以不使用此方案

- **Ocr相关**
  - 测试功能、未来可能移除
  - 选中不精确是因为目标是基于OCR给出的 **Box** 而不是文本
    - 选中一行有间隔是选的 **Word**,选中一行是完整矩形是选择的 **Line**
  - 部分情况高亮区域有重叠,尝试解决但未成功
  - 依赖属于非公开API,所以以下情况均无法解决
  - 其他包括常规的不能识别,识别错误等模型问题
  - 因模型对输入有限制,太大或太小图片会无法识别
    - 目前是将图片进行预处理放大或缩小
    - 缩放加上Ocr本身消耗时间较久,为避免UI阻塞已将其整体移入新线程
    - 但不确定底层库是否线程安全,如调用OCR时有机率闪退,请反馈以供调整逻辑

- **缩略图**
  - **控件已经停止维护, 可能出现BUG, 但没有替代品**
  - **位置偶发性（频繁?）错乱**：
    - 怀疑是控件本身的问题, 难以解决
  - **尺寸调整**：
    - 部分值可能导致定位,移动不正确
      - 设置其他值
    - 偶然会发生部分图片失效的情况
      - 较难复现所以难以针对性解决
      - 暂时最简单的办法是重启程序
  - **图片失败信息出现在缩略图界面**：
    - 边缘情况,请切换下一张图片


- **Exif部分值乱码, 主要原因是EXIF没有编码**

- **ICO是一个容器格式, 图片信息中判定的是Png是正确行为**

- **Avalonia 版本锁死在 11**
  - 12版本移除了程序依赖的核心属性 `ExtendClientAreaChromeHints`
  - 通过各种尝试但仍无力解决

